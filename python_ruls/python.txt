обьект = это фрагмент даннных определёного типа который находитса в памети компьютера.

int = все целые числа

float = все числа с точкой

str = в ('') или в ("") | текст

конкатинація = об'єднення

list = слугують для того щьоб зберигати об'єкти в певному порядку.

set = множина

множина = змінюваний і не упорядкований
	в множинах ми не можимо звертатися до елементів по індексу.

динамічна типізація в пайтон = 

облас визначення = кожна функція визначає 

ООП = виникло наслідок розвидком наслідок ппрцедурного програмуванняв межах якого об"єети і програми не пов"язані

конструктор __init__ - dbkrf'ncz fdnjvfnbxyj d vjvtyn cndjhtyyz trptvgkzhf 

тестування(unit testing) - автономне тестування, коли ми тестуємо код який не залежить від зовнішнього середовищя (не має робити стороніх викликів з програми)

unittest - це коли ми тестуємо частину коду (фонкцію, метод, клас)

запустить тест с терминала - python -m unittest | python -m unittest -v
